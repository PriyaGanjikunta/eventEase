<p style="color: green"><%= notice %></p>


<div id="carts">
  <% @carts.each do |cart| %>
    <%= render cart %>
      <p class="cart-links"><%= link_to "Show this cart", cart %></p>
  <% end %>
</div>

<div class="cart-links">
  <%= link_to "New cart", new_cart_path %>
</div>
